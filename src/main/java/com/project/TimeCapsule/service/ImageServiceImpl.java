package com.project.TimeCapsule.service;
/*
 * import com.project.TimeCapsule.domain.Image; import
 * com.project.TimeCapsule.repository.ImageRepository; import
 * org.springframework.beans.factory.annotation.Autowired; import
 * org.springframework.stereotype.Service; import
 * org.springframework.util.Base64Utils; import
 * org.springframework.web.multipart.MultipartFile;
 * 
 * import java.util.List;
 * 
 * @Service public class ImageServiceImpl implements ImageService { /*
 * 
 * @Autowired private ImageRepository imageRepository;
 * 
 * @Override public Image create(Image image) { return
 * imageRepository.save(image); }
 * 
 * @Override public List<Image> viewAll() { return (List<Image>)
 * imageRepository.findAll(); }
 * 
 * @Override public Image viewById(long id) { return
 * imageRepository.findById(id).orElse(null); }
 * 
 * @Override public String encodeImageBytes(byte[] imageBytes) { return
 * Base64Utils.encodeToString(imageBytes); }
 * 
 * }
 */